# 영속 데이터 볼륨 (벡터, 업로드 문서, 운영 DB)
data/
chroma_db/

# bin/deploy backup pull 로 내려받은 사본
backups/

# Secrets
.env
.env.backup*
deploy/secrets.env
deploy/config.env
deploy/config.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# OS / editor
.DS_Store
.vscode/

# OMC tooling state (local only; plan lives in .omc/plans/)
.omc/
